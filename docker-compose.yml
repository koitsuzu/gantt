services:
  gantt:
    build: .
    container_name: gantt-app
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
    volumes:
      - gantt-data:/app/server/data
    restart: unless-stopped

volumes:
  gantt-data:
    driver: local
